CONFIG_APP_ENABLE=y
CONFIG_APP_PRIORITY=1
CONFIG_APP_STACKSIZE=350

# The Multi-ranger driver stays OUT for the scan. If it were in, its failing
# self-test would stop the firmware booting and the scanner would never run --
# which is the whole problem we are trying to look at.
CONFIG_DECK_MULTIRANGER=n
