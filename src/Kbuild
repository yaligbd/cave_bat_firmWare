# cavebat_v3: the proven flight logic (tag v2-full-mission) plus onboard
# recording and download. Only ONE of these may be built -- they all define
# appMain().
#
#   cavebat.c     the version that flew a complete mission twice
#   cavebat_v2.c  wall following + velocity control. Crashed on takeoff, and
#                 changed too much at once to tell why. Parked, not deleted.
#   cavebat_v3.c  cavebat.c + recording. Change one thing at a time from here.
obj-y += cavebat_v3.o
# obj-y += cavebat.o
# obj-y += cavebat_v2.o
# obj-y += wallfollowing_multiranger_onboard.o
